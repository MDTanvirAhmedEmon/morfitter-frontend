/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "10.0.60.166",
      "192.168.0.118",
    ], // Add the domains you need
  },
  
  
};

export default nextConfig;