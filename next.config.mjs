/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "10.0.60.166",
      "10.0.60.166",
    ], // Add the domains you need
  },


};

export default nextConfig;